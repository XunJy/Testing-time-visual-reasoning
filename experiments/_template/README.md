# Experiment NN: METHOD + MODEL on DATASET

Status: **planned**.

## Identity

- Method:
- Model and exact checkpoint:
- Dataset and split:
- Parent baseline:
- Strict zero-shot or training-based:

## Locked protocol

Record preprocessing, prompts, candidate construction, score normalization,
precision, seeds, validation use, and acceptance criteria before the test run.

## Commands

Add one smoke command and one full command. Both must call reusable code from
`src/ttvr/`; do not copy an earlier experiment implementation into this folder.

## Results

Do not write planned or historical numbers as results from this combination.
Completed runs belong under a new immutable `runs/<run-id>/` directory.
